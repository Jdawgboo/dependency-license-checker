# Dependency License Checker

Check a caller-supplied offline dependency/license manifest against allowed and denied license policy lists.

```bash
cat dependencies.json | python tool.py
python -m unittest -v
```

License metadata is supplied by the caller and may be incomplete. This utility is not legal advice or a complete software-compliance system.
